--------------------------------------------------------------------------------
Selenium with Python
--------------------------------------------------------------------------------
Core Selenium Components
  Selenium WebDriver         | The API that "talks to" and automates web browsers
  Selenium Grid              | An API that allows you to send test to multiple browsers
  Selenium IDE               | A developement environment that allows you to record and playback
                             |
  (Browser) Webdriver        | geckodiver (Firefox)
                             | ChromeDriver (Chrome)
                             | SafariDriver (Safari)
                             |
  API                        | JSON Wire (depricated) 
                             | WebDriver protocol
                             |
  Create Python virtual      | python - m venv <path to new env dir>
  environment                |
                             |
  Activate new env           | source <path to new env dir>/bin/activate
                             |
  Deactivate env             | deactivate (within env dir)
                             |
  install Selenium within    | pip install selenium
  virtual env                |
                             |
  install webdrive and  add to you path
                             |
  import webdriver           | from selenium import webdriver
                             |
  Assign driver to variable  | >> browswer = webdriver.Chrome()   (for Chrome webdriver)
                             |
                             |
                             |
                             |
                             |
                             |
                             |
                             |
